# coreBoard .gitignore — generated and build artifacts only; source is tracked.
# ---------------------------------------------------------------------------
# The build directory must not be committed: it contains compiled objects,
# executables, and CMake/Make/Ninja output that are machine- and tool-dependent.
# Everyone should build from source with CMake.
# ---------------------------------------------------------------------------

# Release notes (internal checklist; not for repo)
RELEASING.md

# Local backup (keep only locally, not in repo)
backup_nohBoard_example.json

# Build directories (do not upload)
build/
out/
cmake-build-*/
*.build/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
/.qmake.cache
/.qmake.stash

# Compiled objects and binaries
*.a
*.lib
*.o
*.obj
*.so
*.so.*
*.dll
*.exe
*.dylib
*.core
core
!core/

# Qt / MOC / UIC / RCC generated
*.moc
moc_*.cpp
ui_*.h
qrc_*.cpp
*_pch.h.cpp

# Make / Ninja
Makefile
Makefile.*
*.ninja
*.ninja_deps
*.ninja_log

# Qt project / IDE
*.pro.user*
CMakeLists.txt.user*
*.autosave

# Windows / Visual Studio
*.idb
*.ilk
*.pdb
*.sln
*.suo
*.vcxproj
*vcxproj.*
*.vcxproj.user
*.ncb
*.sdf
*.opensdf
Thumbs.db
*.res
*.rc
*_resource.rc
!CoreBoard.rc

# MinGW
*.Debug
*.Release
*.ib_pdb_index

# macOS
*.app
.DS_Store

# Linux / misc
.directory
*.debug
*.orig
*.rej
.#*
*.*#
tags

# Editors
*~
.*.swp
*.flc

# Python
*.pyc

